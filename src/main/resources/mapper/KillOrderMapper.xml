<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhuang.kill.mapper.KillOrderMapper">
    <insert id="insert" parameterType="com.zhuang.kill.entity.KillOrder">
        INSERT INTO kill_order (kill_order_id, user_id, order_id, item_id)
        VALUES (#{killOrderId}, #{userId}, #{orderId}, #{itemId})
    </insert>
</mapper>
